"""
Robot Bringup Launch — Auto-generated by Hermod
@ai-context: Launches all core nodes for 六轴协作机械臂.
Includes ros2_control, sensor drivers, and state publishers.
@ai-extend: Add custom nodes (navigation, perception, etc.) below.
ROS Version: humble
Namespace:   arm_manipulator
"""
from launch import LaunchDescription
from launch_ros.actions import Node
from launch.actions import DeclareLaunchArgument
def generate_launch_description():
ns = "arm_manipulator"
# ── ros2_control ──
controller_manager = Node(
package="controller_manager",
executable="ros2_control_node",
namespace=ns,
parameters=["$(find-pkg-share arm_manipulator_bringup)/config/controllers.yaml"],
output="screen",
)
# ── Sensor Drivers ──
nodes = [controller_manager]


# TODO: Add driver node for base_imu (bmi088)


return LaunchDescription(nodes)