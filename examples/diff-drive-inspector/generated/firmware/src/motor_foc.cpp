/**
* Motor FOC Control — Auto-generated by Hermod
*
* @ai-context: Field-Oriented Control implementation for all active joints.
*              PID parameters are knowledge base defaults — tune on real hardware.
* @ai-critical: PID gains marked @ai-critical require human confirmation to modify.
* @ai-extend: Add custom control modes (e.g. impedance control) below.
*/
#include "motor_foc.h"
#include "can_config.h"
#include "safety/limits.h"
/* ── Per-Motor PID Configurations ── */

/**
* left_front_wheel — dji-m3508 FOC Controller
*
* Pole pairs: 7 (hardware parameter)
* Reduction ratio: 19.2:1
*
* @ai-critical: Current loop gains — modify only with torque sensor verification
* @ai-critical: Speed loop gains — modify only with encoder feedback verification
*/
static const FocController_t foc_left_front_wheel = {
.pole_pairs        = 7,     /* @ai-lock */
.reduction_ratio   = 19.2f,   /* @ai-lock */

.current_kp        = 0.8f,   /* @ai-critical */
.current_ki        = 0.05f,   /* @ai-critical */
.current_kd        = 0f,   /* @ai-critical */


.speed_kp          = 0.5f,   /* @ai-critical */
.speed_ki          = 0.1f,   /* @ai-critical */
.speed_kd          = 0.02f,   /* @ai-critical */


.position_kp       = 2f,   /* @ai-critical */
.position_ki       = 0f,   /* @ai-critical */
.position_kd       = 0.1f,   /* @ai-critical */

.max_current_a     = SAFE_LEFT_FRONT_WHEEL_MAX_CURRENT_A,
.soft_current_a    = SAFE_LEFT_FRONT_WHEEL_SOFT_CURRENT_A,
.max_temp_c        = SAFE_LEFT_FRONT_WHEEL_MAX_TEMP_C,
};

/**
* right_front_wheel — dji-m3508 FOC Controller
*
* Pole pairs: 7 (hardware parameter)
* Reduction ratio: 19.2:1
*
* @ai-critical: Current loop gains — modify only with torque sensor verification
* @ai-critical: Speed loop gains — modify only with encoder feedback verification
*/
static const FocController_t foc_right_front_wheel = {
.pole_pairs        = 7,     /* @ai-lock */
.reduction_ratio   = 19.2f,   /* @ai-lock */

.current_kp        = 0.8f,   /* @ai-critical */
.current_ki        = 0.05f,   /* @ai-critical */
.current_kd        = 0f,   /* @ai-critical */


.speed_kp          = 0.5f,   /* @ai-critical */
.speed_ki          = 0.1f,   /* @ai-critical */
.speed_kd          = 0.02f,   /* @ai-critical */


.position_kp       = 2f,   /* @ai-critical */
.position_ki       = 0f,   /* @ai-critical */
.position_kd       = 0.1f,   /* @ai-critical */

.max_current_a     = SAFE_RIGHT_FRONT_WHEEL_MAX_CURRENT_A,
.soft_current_a    = SAFE_RIGHT_FRONT_WHEEL_SOFT_CURRENT_A,
.max_temp_c        = SAFE_RIGHT_FRONT_WHEEL_MAX_TEMP_C,
};

/**
* left_rear_wheel — dji-m3508 FOC Controller
*
* Pole pairs: 7 (hardware parameter)
* Reduction ratio: 19.2:1
*
* @ai-critical: Current loop gains — modify only with torque sensor verification
* @ai-critical: Speed loop gains — modify only with encoder feedback verification
*/
static const FocController_t foc_left_rear_wheel = {
.pole_pairs        = 7,     /* @ai-lock */
.reduction_ratio   = 19.2f,   /* @ai-lock */

.current_kp        = 0.8f,   /* @ai-critical */
.current_ki        = 0.05f,   /* @ai-critical */
.current_kd        = 0f,   /* @ai-critical */


.speed_kp          = 0.5f,   /* @ai-critical */
.speed_ki          = 0.1f,   /* @ai-critical */
.speed_kd          = 0.02f,   /* @ai-critical */


.position_kp       = 2f,   /* @ai-critical */
.position_ki       = 0f,   /* @ai-critical */
.position_kd       = 0.1f,   /* @ai-critical */

.max_current_a     = SAFE_LEFT_REAR_WHEEL_MAX_CURRENT_A,
.soft_current_a    = SAFE_LEFT_REAR_WHEEL_SOFT_CURRENT_A,
.max_temp_c        = SAFE_LEFT_REAR_WHEEL_MAX_TEMP_C,
};

/**
* right_rear_wheel — dji-m3508 FOC Controller
*
* Pole pairs: 7 (hardware parameter)
* Reduction ratio: 19.2:1
*
* @ai-critical: Current loop gains — modify only with torque sensor verification
* @ai-critical: Speed loop gains — modify only with encoder feedback verification
*/
static const FocController_t foc_right_rear_wheel = {
.pole_pairs        = 7,     /* @ai-lock */
.reduction_ratio   = 19.2f,   /* @ai-lock */

.current_kp        = 0.8f,   /* @ai-critical */
.current_ki        = 0.05f,   /* @ai-critical */
.current_kd        = 0f,   /* @ai-critical */


.speed_kp          = 0.5f,   /* @ai-critical */
.speed_ki          = 0.1f,   /* @ai-critical */
.speed_kd          = 0.02f,   /* @ai-critical */


.position_kp       = 2f,   /* @ai-critical */
.position_ki       = 0f,   /* @ai-critical */
.position_kd       = 0.1f,   /* @ai-critical */

.max_current_a     = SAFE_RIGHT_REAR_WHEEL_MAX_CURRENT_A,
.soft_current_a    = SAFE_RIGHT_REAR_WHEEL_SOFT_CURRENT_A,
.max_temp_c        = SAFE_RIGHT_REAR_WHEEL_MAX_TEMP_C,
};

/* ── Controller Registry ── */
#define NUM_MOTORS  4
static const FocController_t* g_controllers[NUM_MOTORS] = {

&foc_left_front_wheel,

&foc_right_front_wheel,

&foc_left_rear_wheel,

&foc_right_rear_wheel,

};
/* ── @ai-extend: Add custom control logic below ── */